const LayoutInputLabel = ({ children }) => {
    return (
        <div className='flex flex-col gap-3' id='input-form-style'>
            {children}
        </div>
    );
};

export default LayoutInputLabel;
